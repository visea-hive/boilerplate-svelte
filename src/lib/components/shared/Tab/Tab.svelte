<script lang="ts">
	import { Tabs as TabsPrimitive } from "bits-ui";
    import { cn } from "$lib/utils";

	let { 
		ref = $bindable(null),
		value = $bindable(),
        class: className = "",
		children,
		...restProps
	}: TabsPrimitive.RootProps = $props();
</script>

<TabsPrimitive.Root bind:ref bind:value={value as never} class={cn("w-full", className)} {...restProps}>
	{@render children?.()}
</TabsPrimitive.Root>
